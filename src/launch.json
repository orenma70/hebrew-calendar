{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch Chrome against localhost",
      "url": "http://localhost:5173", // Use 3000 if using Create React App
      "webRoot": "${workspaceFolder}"
    }
  ]
}